patch dwm with https://dwm.suckless.org/patches/autostart/
>$ patch --merge -i patchname-165987165986.diff

>$ cd .dwm
>$ sudo vim autostart.sh
add stuff to the script
>$ sudo chmod +x autostart.sh

restart dwm
